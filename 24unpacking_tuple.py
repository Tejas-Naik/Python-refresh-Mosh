# Tuple Unpacking is useful when you wanna use the values of the tuple as a single variables
values_tuple = (10, 20, 30)
x, y, z = values_tuple

print(x)
print(y)
print(z)
# Take care that the variables on the right are = length/items in tuple

